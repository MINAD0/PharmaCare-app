name: pharmacare
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.5.4

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2
  flutter_secure_storage: ^8.0.0
  image_picker: ^0.8.7+5
  path_provider: ^2.0.11

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0


flutter:
  uses-material-design: true

  # Define your assets here
  assets:
    - images/logo.png # Store your logo in an `assets/` folder.
    - images/pills.png
    - images/chat_bubble.png
    - images/ordonnance.png